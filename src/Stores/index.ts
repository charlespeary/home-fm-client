export { initialState } from "./initialState";
export { store } from "./store";
import { FetchStatus, TokenStatus, Token } from "../Actions/index";




export interface ReduxState {
    token: Token,
}

export interface Action {

}